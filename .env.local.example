# ==============================================================================
# HypeLens AI - Environment Variables Template
# ==============================================================================
#
# This file serves as a template for environment variables required by the
# HypeLens AI frontend application. To use this file:
#
# 1. Copy this file to .env.local:
#    cp .env.local.example .env.local
#
# 2. Update the values in .env.local with your actual configuration
#
# 3. Never commit .env.local to version control (it's in .gitignore)
#
# IMPORTANT: The backend FastAPI server must be running on the specified
# API URL before starting the frontend development server.
#
# ==============================================================================

# ------------------------------------------------------------------------------
# REQUIRED VARIABLES
# ------------------------------------------------------------------------------

# Backend API Base URL
# This should point to your FastAPI backend server
# Development: http://localhost:5000
# Production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:5000

# ------------------------------------------------------------------------------
# OPTIONAL VARIABLES
# ------------------------------------------------------------------------------

# Application Branding
# Used for display in the UI and page titles
# Default: "HypeLens AI"
# NEXT_PUBLIC_APP_NAME=HypeLens AI

# Application Version
# Displayed in the footer and about section
# Default: "1.0.0"
# NEXT_PUBLIC_APP_VERSION=1.0.0

# Analytics Configuration
# Enable or disable analytics tracking (Google Analytics, etc.)
# Default: false
# Accepted values: "true" or "false" (as string)
# NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Image Upload Configuration
# Maximum image upload size in bytes
# Default: 10485760 (10 MB)
# Examples:
#   5 MB = 5242880
#   10 MB = 10485760
#   20 MB = 20971520
# NEXT_PUBLIC_MAX_IMAGE_SIZE=10485760

# Search Configuration
# Default number of search results to display per page
# Default: 20
# NEXT_PUBLIC_DEFAULT_SEARCH_LIMIT=20

# ------------------------------------------------------------------------------
# DEVELOPMENT VARIABLES (Automatically set by Next.js)
# ------------------------------------------------------------------------------

# Node Environment
# Automatically set by Next.js - DO NOT set manually
# Values: "development" | "production" | "test"
# NODE_ENV=development

# ------------------------------------------------------------------------------
# SECURITY NOTES
# ------------------------------------------------------------------------------
#
# - Never commit .env.local with actual values to version control
# - Keep sensitive API keys and secrets in .env.local only
# - Use different values for development, staging, and production environments
# - Rotate credentials regularly and update .env.local accordingly
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Only use NEXT_PUBLIC_ prefix for non-sensitive configuration values
#
# ------------------------------------------------------------------------------

# ------------------------------------------------------------------------------
# SECURITY HEADERS CONFIGURATION
# ------------------------------------------------------------------------------

# Production API Domain (for CSP connect-src directive)
# Only needed in production deployments
# Development: Uses http://localhost:5000 (automatically configured)
# Production: Set to your actual API domain
# NEXT_PUBLIC_PRODUCTION_DOMAIN=https://api.yourdomain.com

# Note: Security headers are automatically applied by next.config.ts
# The following security headers are configured:
#   - Content Security Policy (CSP): Prevents XSS attacks
#   - X-Frame-Options: Prevents clickjacking
#   - X-Content-Type-Options: Prevents MIME-sniffing
#   - Referrer-Policy: Controls referrer information
#   - Permissions-Policy: Restricts browser features
#   - Strict-Transport-Security (HSTS): Forces HTTPS in production

# Note: CORS is handled by the backend (app.py) - do NOT configure in frontend
# The FastAPI backend already has proper CORS middleware configured to allow
# requests from http://localhost:3000 and http://127.0.0.1:3000

# ------------------------------------------------------------------------------
